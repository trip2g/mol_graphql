type Query {
	viewer: User!
	notes: [Note!]!
}

type Mutation {
	note_like(id: ID!): Note!
}

type Subscription {
	note_liked: Note!
}

type Note {
	id: ID!
	title: String!
	body: String!
	likes: Int!
	author: User!
}

type User {
	id: ID!
	name: String!
	pinned_note: Note
}
